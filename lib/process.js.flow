// @flow
'use strict';

const cp = require('child_process');

const util = require('util');

import type { spawnResult } from './index';
declare var spawnAsync: (command: string, args: ?Array<string>, options: child_process$spawnOpts) => Promise<spawnResult>; // Process spawning stuff

declare var spawnRes: (command: string, args: ?Array<string>, options: ?child_process$spawnSyncOpts) => boolean; // Process spawning stuff

declare var spawnResAsync: (command: string, args: ?Array<string>, options: ?child_process$spawnOpts) => Promise<boolean>;
module.exports = {
  spawnAsync,
  spawnRes,
  spawnResAsync
};